.idea/
.vscode/
*.DS_Store
.design_handoff/
*.zip
.env
*.env
.claude/

# Claude Code project instructions — internal working notes for the AI
# assistant, not meant for the public repo. Matches at any depth (root,
# frontend/, backend/), unlike a leading-slash pattern.
CLAUDE.md

# Python
__pycache__/
*.pyc

# Purchased book sources + anything derived from them for the "Study from
# Book" feature — personal copies, not for redistribution in the public repo.
book-sources/
backend/data/books/

# Offline textbook extraction artifacts. Source PDFs and OCR output must never
# be committed; the reusable parser code lives in textbook-parser/.
textbook-parser/work/
